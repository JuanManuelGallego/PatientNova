meta {
  name: Update Patient
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/patients/{{patientId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "notes": "Updated notes after first session.",
    "status": "ACTIVE"
  }
}
